# Local Git configuration (not committed to git)
# Lives at ~/.config/git/config.local as a real file, outside the git working tree.

[user]
    name = Your Name
    email = your.email@example.com
    # signingkey = YOUR_GPG_OR_SSH_KEY_ID

# [commit]
#     gpgsign = true
