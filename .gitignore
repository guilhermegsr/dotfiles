# Compiled zsh bytecode and dumps
*.zwc
*.zwc.old
.zcompdump*
zcompdump*

# Mise uses rolling selectors; lockfile generation is intentionally disabled
/mise/mise.lock

# Backups
*.bak*
dotfiles-backup-*.tar.gz*
*.age
*.enc

# Local overrides and secrets: identity, credential helpers, tokens
*.local
*.local.zsh
zsh/local.zsh

# OS metadata
.DS_Store
Thumbs.db
